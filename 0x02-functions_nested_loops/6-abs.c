/**
 * _abs - computes the absolute value of a number
 * @n: value to be computed
 *
 * Return: the absolute value of n
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}

	else
	{
		return (n * -1);
	}

}
